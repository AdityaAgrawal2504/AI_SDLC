<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">
    <Properties>
        <Property name="LOG_PATTERN">
            %d{yyyy-MM-dd HH:mm:ss.SSS} %level [%t] [%C{1}.%M] %msg%n%throwable
        </Property>
        <Property name="APP_LOG_ROOT">./logs</Property>
        <Property name="KAFKA_BOOTSTRAP_SERVERS">localhost:9092</Property>
        <Property name="KAFKA_TOPIC">app-logs-conversations</Property>
    </Properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT" follow="true">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>

        <RollingFile name="File"
                     fileName="${APP_LOG_ROOT}/conversations-api.log"
                     filePattern="${APP_LOG_ROOT}/conversations-api-%d{yyyy-MM-dd}-%i.log.gz">
            <JsonLayout complete="false" compact="true" properties="true" />
            <Policies>
                <SizeBasedTriggeringPolicy size="10MB"/>
                <TimeBasedTriggeringPolicy/>
            </Policies>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>
        
        <!-- 
        Abstracted Kafka Appender. 
        Application code uses standard Log4j Logger. This configuration routes logs to Kafka.
        This can be swapped for an EventHub appender or others without changing Java code.
        -->
        <Kafka name="Kafka" topic="${KAFKA_TOPIC}" >
            <Property name="bootstrap.servers">${KAFKA_BOOTSTRAP_SERVERS}</Property>
            <JsonLayout complete="false" compact="true" properties="true" />
        </Kafka>

    </Appenders>

    <Loggers>
        <Logger name="com.example.conversations.api" level="info" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="File"/>
            <!-- <AppenderRef ref="Kafka"/> --> <!-- Uncomment to enable Kafka logging -->
        </Logger>

        <Root level="warn">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="File"/>
            <!-- <AppenderRef ref="Kafka"/> -->
        </Root>
    </Loggers>
</Configuration>
```
```properties
# application-Conversations-GCAPI_9876.properties
server.port=8080
spring.application.name=conversations-api-gcapi-9876

# Specify the custom Log4j2 configuration file
logging.config=classpath:log4j2-Conversations-GCAPI_9876.xml

# Security properties (placeholders)
jwt.secret.key=bXktc3VwZXItc2VjcmV0LWtleS1mb3ItandrLWF1dGhlbnRpY2F0aW9uLXdpdGgtZW5vdWdoLWVudHJvcHk=
jwt.expiration.ms=86400000 # 24 hours

# API specific configurations
api.pagination.default-limit=20
api.pagination.max-limit=100
```
```java
// src/main/java/com/example/conversations/api/ConversationsApiApplicationGCAPI_9876.java