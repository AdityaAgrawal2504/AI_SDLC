<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">
    <Properties>
        <Property name="LOG_PATTERN">
            %d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%t] %c{1.} - %msg%n
        </Property>
        <Property name="LOG_FILE_PATH">./logs</Property>
        <Property name="LOG_FILE_NAME">auth-service</Property>
    </Properties>

    <Appenders>
        <!-- Console Appender for local development -->
        <Console name="Console" target="SYSTEM_OUT" follow="true">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>

        <!-- Rolling File Appender for structured JSON logs -->
        <RollingFile name="File"
                     fileName="${LOG_FILE_PATH}/${LOG_FILE_NAME}.log"
                     filePattern="${LOG_FILE_PATH}/${LOG_FILE_NAME}-%d{yyyy-MM-dd}-%i.log.gz">
            <JsonLayout complete="false" compact="true" eventEol="true" properties="true">
                 <KeyValuePair key="timestamp" value="$${date:yyyy-MM-dd'T'HH:mm:ss.SSS'Z'}"/>
            </JsonLayout>
            <Policies>
                <SizeBasedTriggeringPolicy size="10MB"/>
                <TimeBasedTriggeringPolicy/>
            </Policies>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>

        <!--
          Abstracted logging layer placeholders for external systems.
          These would require additional dependencies (e.g., log4j-kafka-appender).
        -->

        <!-- Kafka Appender (Placeholder) -->
        <!--
        <Kafka name="Kafka" topic="auth-logs">
            <PatternLayout pattern="%m" />
            <Property name="bootstrap.servers">kafka-broker1:9092,kafka-broker2:9092</Property>
        </Kafka>
        -->

        <!-- Azure Event Hub Appender (would require a custom appender or a library) -->
        <!--
        <EventHubAppender name="EventHub">
            <ConnectionString>Endpoint=sb://...;</ConnectionString>
            <EventHubName>auth-logs-hub</EventHubName>
            <JsonLayout ... />
        </EventHubAppender>
        -->

    </Appenders>

    <Loggers>
        <Logger name="com.example.auth" level="info" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="File"/>
            <!-- <AppenderRef ref="Kafka"/> -->
        </Logger>

        <Logger name="org.springframework.web" level="info" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="File"/>
        </Logger>

        <Root level="warn">
            <AppenderRef ref="Console"/>
        </Root>
    </Loggers>
</Configuration>
```
src/main/java/com/example/auth/AuthServiceApplication.java
```java